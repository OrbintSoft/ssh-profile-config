<!-- markdownlint-disable-file MD041 -- a PR body fragment does not start with an H1 -->
## Summary

Describe what this change does and why.

## Checklist

- [ ] Every commit is signed off (`git commit -s`) — this certifies the DCO
      (`DCO.txt`) and accepts the CLA (`CLA.md`). See `CONTRIBUTING.md` if the
      DCO check fails.
- [ ] `make lint` passes.
